# 📋 Environment-Variablen Dokumentation
#
# ℹ️ BEST PRACTICE: Diese .env.example Datei ist Standard in vielen Projekten!
#
# Warum gibt es diese Datei?
# - Dokumentiert alle verfügbaren Environment-Variablen
# - Wird auf GitHub committet (Standard-Practice)
# - Zeigt anderen Entwicklern, welche Config-Optionen existieren
# - Dient als Referenz für eigene .env Dateien
#
# WICHTIG: Diese Datei wird NICHT von Vite geladen!
# Sie ist nur Dokumentation. Die echten Config-Dateien sind:
# - .env.local (für npm run dev)
# - .env.production (für npm run build)

# ========================================
# Verfügbare Variablen:
# ========================================

# Upload-Funktion aktivieren (true/false)
VITE_ENABLE_UPLOAD=true

# Pfad zu den Daten
VITE_DATA_PATH=./data/local
# Optionen:
# - ./data/local   → für lokale Experimente
# - ./data/corpus  → kuratierter Datensatz (wird auf GitHub Pages deployed)

# ========================================
# Welche .env Datei wird wann verwendet?
# ========================================

# npm run dev       → lädt .env.local (Upload AN, data/local/)
# npm run build     → lädt .env.production (Upload AUS, data/corpus/)
# npm run preview   → lädt .env.production (Upload AUS, data/corpus/)

# Eigene Overrides? Erstelle .env.development (wird nicht committet)
